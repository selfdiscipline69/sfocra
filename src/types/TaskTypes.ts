export interface TaskItem {
  text: string;
  image: string | null;
  completed: boolean;
  showImage: boolean;
}

export interface WeeklyTrialItem {
  text: string;
  image: string | null;
  completed: boolean;
  showImage: boolean;
}